TOOL_INFO = {
    "add_file": {
        "description": "Adds an exact file name to the timeline or canvas from the available files. Does not work with any category. The file should have an extension like .mp4 or .jpg for this took to be selected. Example queries include - add barbie.png to the timeline, add subham.wav",
        "params": {
            "fileName": {"type": "string", "description": "The name of the file to add. For example, if the user says 'add the file epic.mp4', this value should be 'epic.mp4'."}
        }
    }
} 