TOOL_INFO = {
    "file_classify": {
        "description": "Classifies and selects the best matching files from available files based on a query string using AI image classification. USE IF THERE IS NO EXTENSION IN THE FILE NAME. Example queries include - add all cats to the timeline, add trees to the timeline. Add images containing a long man.",
        "params": {
            "query": {"type": "string", "description": "The search query to find the most relevant files. For example, if the user says 'find images of people', this value should be 'people'."}
        }
    }
}
