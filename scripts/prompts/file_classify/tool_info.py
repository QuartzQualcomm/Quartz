TOOL_INFO = {
    "file_classify": {
        "description": "Classifies and selects the best matching files from available files based on a query string using AI image classification.",
        "params": {
            "query": {"type": "string", "description": "The search query to find the most relevant files. For example, if the user says 'find images of people', this value should be 'people'."}
        }
    }
}
